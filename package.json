{
  "name": "heatmap-analytics",
  "private": true,
  "type": "module",
  "description": "Self-hosted heatmap + session replay analytics on Cloudflare Workers + D1. ~1.5KB gzipped tracker, no cookies, one anonymous localStorage id.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute heatmap-analytics --file=schema.sql --remote",
    "db:migrate": "wrangler d1 execute heatmap-analytics --remote",
    "db:schema:local": "wrangler d1 execute heatmap-analytics --file=schema.sql",
    "test": "pnpm run test:unit && pnpm run test:e2e",
    "test:unit": "node --test 'test/**/*.test.ts'",
    "test:e2e": "HM_ENDPOINT= pnpm run build && playwright test",
    "test:e2e:ui": "HM_ENDPOINT= pnpm run build && playwright test --ui",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^26.2.0",
    "esbuild": "^0.25.5",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.2",
    "vite": "^8.2.1",
    "vite-plugin-singlefile": "^2.3.3",
    "wrangler": "^4.27.0"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}
